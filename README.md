# dotfiles

```bash
chmod +x ./activate.sh
./activate.sh
```

If you want to run the steps separately:

```bash
chmod +x ./basic-install.sh ./reset-to-bash-ohmybash.sh
./basic-install.sh
./reset-to-bash-ohmybash.sh
```
